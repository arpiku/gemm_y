# TODO.md — Active kernel validation and benchmark workflow

## Current state

- `k1_smem` is implemented in `src/sm120/gemm_bf16_k1_smem.cu` and is the active `sm120` bf16 optimization kernel.
- `k1_smem` is registered in the full benchmark and focused smoke test.
- `k1_smem` passed the current smoke sizes `{32, 96, 128}` with zero reported relative error.
- `k0_ldg` and `k0_coalesced` have completed benchmark results and should become smoke-only validation controls.
- `NaiveGemm` remains the generic scalar baseline; it is not part of the active `k1_` experiment.
- `db/gemm_y.db` is not modified or regenerated by kernel development. The user ingests selected benchmark data manually.

## 1. Separate active benchmark kernels from completed controls

### Intended registry semantics

The existing profiler API remains unchanged. Registration lists determine executable role:

- The full `gemm_y` benchmark registers the cuBLAS reference implicitly, `NaiveGemm` as the generic baseline, and active experimental kernels only.
- The focused `kernel_smoke` executable registers active kernels plus completed controls needed for regression coverage.
- A completed kernel is removed from the full benchmark registry, not deleted from source or historical result storage.
- Registration must be explicit; do not use commented-out registration lines or runtime flags for this lifecycle state.

### Minimal code changes

- [ ] In `src/main.cpp`, add a small bf16 benchmark-registration helper or local registration block that keeps:
  - `NaiveGemm<T>` as the generic baseline.
  - `k1_smem` as the active `sm120` experiment.
  - `k0_ldg` and `k0_coalesced` out of the full benchmark.
- [ ] Preserve the existing metadata collection so the full CSV and `.meta` sidecar describe exactly the kernels executed by that run.
- [ ] In `tests/kernel_smoke.cu`, keep registering `k0_ldg`, `k0_coalesced`, and `k1_smem`.
- [ ] Keep the smoke row-count and per-size name checks aligned with the smoke registry: cuBLAS + three custom kernels.
- [ ] Do not change `Profiler`, CMake source discovery, CSV schema, or database tooling for this lifecycle change.
- [ ] Do not modify `db/gemm_y.db`; preserve CSV and metadata files for the user's manual ingestion decision.

### Documentation contract

- [ ] Update `AGENTS.md` to state that full-benchmark registration and smoke/regression registration may differ.
- [ ] State that removing a kernel from the full benchmark does not delete its implementation or historical results.
- [ ] State that completed kernels may remain smoke-only controls and that registration lists must be explicit and reproducible.
- [ ] Keep this lifecycle policy in `AGENTS.md`; do not retain completed implementation history in this file after the transition is complete.

## 2. Complete `k1_smem` validation

- [ ] Build the focused target:

  ```sh
  cmake --build build --target kernel_smoke -j
  ```

- [ ] Run `./build/kernel_smoke` after the registry change.
- [ ] Add a focused padded-leading-dimension correctness case for `k1_smem` if the existing profiler path only exercises contiguous `ld == rows` matrices.
  - Keep this local to the smoke/test path; do not redesign `Matrix` or `Profiler`.
  - Allocate padded device buffers with `Buffer<T, Space::Device>` and construct `MatrixView` descriptors directly with `ld > rows`.
  - Compare the result against cuBLAS or the existing accuracy helper.
- [ ] Include tile-boundary cases: sizes smaller than 64, non-multiples of 64, and K values not divisible by 16.
- [ ] Run the full test suite:

  ```sh
  ctest --test-dir build
  ```

## 3. Active `k1_smem` benchmark

- [ ] Run the normal full executable after only `NaiveGemm` and `k1_smem` remain in the benchmark registry.
- [ ] Confirm the output row count and `.meta` kernel list contain cuBLAS, `naive`, and `k1_smem`, but not `k0_ldg` or `k0_coalesced`.
- [ ] Compare `k1_smem` against cuBLAS and the retained historical k0 results using the generated CSVs; do not rerun completed k0 controls in the full sweep.
- [ ] Keep the current `k1_smem` tile configuration unchanged while establishing the first complete benchmark result.
- [ ] Change one optimization variable at a time only after the baseline result is recorded.
- [ ] The user runs the clock-locked benchmark wrapper and manually ingests the selected result; do not run `sudo` or mutate `db/gemm_y.db`.

## Completion criteria

- [ ] Full bf16 benchmark runs only the generic baseline and active `k1_smem` experiment.
- [ ] `kernel_smoke` continues to validate `k0_ldg`, `k0_coalesced`, and `k1_smem`.
- [ ] Completed k0 implementations remain available as source and smoke controls.
- [ ] `k1_smem` passes contiguous and padded-leading-dimension correctness checks.
- [ ] `ctest --test-dir build` passes.
- [ ] Full benchmark output contains no newly generated k0 rows.
- [ ] `db/gemm_y.db` remains unchanged until the user explicitly ingests a selected run.
